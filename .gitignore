# Datos locales — nunca a GitHub
data/
venv/
__pycache__/
*.db
*.db-wal
*.db-shm
*.pyc
apk/.gradle/
apk/app/build/
# Túnel Cloudflare: el token vive en ~/.cloudflared/ (fuera del repo). Por si acaso:
tunel/*.env

# Herramientas de map-matching: config generado, pid y logs
tools/runtime/

# APK firmada: canal de entrega es Nextcloud, nunca el git
apk/trackcam-release.apk
